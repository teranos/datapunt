---
name: cue
description: Load before saying anything about CUE or proposing any CUE.
---

Nothing about CUE comes from memory. Run the question as a minimal `.cue` file through `scripts/cue` and show file, command and output. Quote the spec only from `cue-lang/cue` `doc/ref/spec.md` at the tag `scripts/cue version` prints.
